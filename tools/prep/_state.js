/* Общий кусок: кладём состояние экзамена в localStorage и перезагружаемся —
   так попадаем на ЛЮБОЙ шаг сразу, без прокликивания.
   Индексы шагов (посчитаны из assets/data.js, всего 58):
     1 — сопоставление букв (арабский)      32 — составление слова
     3 — счёт слогов                        40 — да/нет
    24 — сифаты (галочки)                   57 — чтение вслух и запись
   Меняй STEP под нужный экран. Тему добавляй строкой localStorage.setItem
   ('tajweed_theme','dark') рядом — обе части идут ДО перезагрузки. */
